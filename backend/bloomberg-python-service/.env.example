# Bloomberg Python Service Environment Configuration

Create a `.env` file in `backend/bloomberg-python-service/` with:

```
BLOOMBERG_HOST=localhost
BLOOMBERG_PORT=8194
FLASK_PORT=5000
FLASK_ENV=development
```

**Note:** This file is gitignored for security. Copy this configuration manually.
